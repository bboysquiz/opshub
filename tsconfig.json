{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "baseUrl": ".",
    "types": ["vitest/globals"]
  },
  "include": ["packages/**/*.ts", "packages/**/*.spec.ts", "tests/**/*.ts"],
  "exclude": ["node_modules", "dist", "coverage", "apps", "server"]
}
